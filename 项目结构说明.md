# 咕咕个人作品集 — 项目结构说明 (V3.4)

## A. 完整目录树

```
D:\cc\咕咕的个人作品集\
├── index.html                      [MODIFIED] 主文件 — 单HTML架构，包含所有内容
├── Carla_Portfolio_V3.3.html       [UNCHANGED] V3.3 原始备份（视觉基准，不要修改）
├── 架构图.jpg                      [UNCHANGED] 信息架构参考图
├── .claude/                        [UNCHANGED] Claude 工作目录
│   └── settings.local.json
└── 项目结构说明.md                 [NEW] 本文档
```

**关键说明：**
- 本项目是**单文件架构**，未迁移到 React/Vite 多文件结构
- 理由：V3.3 是视觉基准，迁移会破坏现有设计，且无功能必要性
- 所有内容（HTML/CSS/JS/base64图片）都在 `index.html` 内部
- 数据驱动：通过编辑脚本顶部的配置对象来更新内容

---

## B. index.html 文件职责

`index.html` 是整个网站的核心，分为以下部分：

### 1. HTML 结构区（第1-373行）
- `<head>` — 字体、CSS样式、meta信息
  - **V3.3 原始样式**（第7-196行）— 不要修改
  - **V3.4 附加样式**（第198-263行）— 详情页、移动端修复、back-to-top
- `<body>` — 页面内容
  - **Header**（第211-218行）— 固定导航栏
  - **Main**（第220-367行）— Home 长滚动页面
    - Hero 区
    - Projects 目录
    - Research 目录（现已可点击）
    - AI Lab 目录（现已可点击）
    - Thinking 目录（现已可点击）
    - About 区
    - Resume 区
  - **Detail View 容器**（第369行）— 动态渲染详情页
  - **Back-to-Top 按钮**（第370行）
  - **Footer**（第372-376行）

### 2. JavaScript 数据配置区（第379-447行）
**这是内容的唯一来源，编辑这里即可更新作品集**

#### 核心配置对象：

**`DATA`**（第379行）
- 项目文本内容（标题、描述、工作范围、档案章节）
- 三个项目：`lovememo` / `personal` / `vybcall`
- 每个项目包含：`title, kicker, tagline, problem, work[], sections[]`

**`IMGS`**（第380行）
- 项目封面图（base64编码）
- 三个键：`lovememo` / `personal` / `vybcall`

**`LINKS`**（第407-415行）
- 旧版简单链接配置（已被 RESOURCES 取代，但保留以兼容DATA）

**`RESOURCES`**（第422-446行）⭐ **重要**
- 每个项目的资源文件配置
- 每个资源条目包含：
  - `type` — 资源类型（demo/github/figma/prd/prototype/tracking等）
  - `label` — 按钮显示文字
  - `sub` — 按钮副标题
  - `url` — 真实链接（null = 显示"Coming soon"占位）

**`ARTICLES`**（第448-461行）⭐ **重要**
- Research / AI Lab / Thinking 文章配置
- 三个分类：`research` / `lab` / `thinking`
- 每个分类包含：
  - `kicker` — 页眉标签
  - `back` — 返回锚点
  - `items{}` — 文章条目（no, title, desc）

**`SITE`**（第463行）
- 全局站点配置
- `resumePdf` — 简历PDF路径（null = 显示占位按钮）

### 3. JavaScript 引擎区（第465-523行）
**无需修改，除非要改功能逻辑**

- `renderProject()` — 渲染项目详情页
- `renderArticle()` — 渲染文章详情页
- `route()` — hash 路由引擎
- `openProject()` / `openArticle()` — 导航函数
- `initResume()` — 简历按钮初始化
- 事件监听器（滚动、键盘、路由）

---

## C. 本次修改文件

### [MODIFIED] index.html
- **新增内容：**
  - V3.4 样式块（详情页、移动端导航修复、back-to-top）
  - Research/Lab/Thinking 行的 `onclick` 事件
  - Detail view 容器
  - Back-to-top 按钮
  - 配置对象 `RESOURCES` / `ARTICLES` / `SITE`
  - 路由引擎和渲染函数
- **修改内容：**
  - Resume 按钮区域改为动态容器
  - 项目卡片保持原有 `onclick="openProject(...)"` 调用
  - 移除旧 modal 弹窗代码（已被全页详情视图取代）
- **保持不变：**
  - V3.3 的 Home 视觉设计（Hero/排版/字体/颜色/装饰元素）
  - Header 导航栏
  - Footer
  - 原有 CSS 样式（仅追加，不覆盖）

### [NEW] 项目结构说明.md
- 本文档

### [UNCHANGED] Carla_Portfolio_V3.3.html
- V3.3 视觉基准备份
- **永远不要修改此文件**

### [UNCHANGED] 架构图.jpg
- 信息架构参考图

---

## D. 项目资源管理方式

### 当前资源位置

#### 项目封面图
- **存储位置**：`index.html` 内部，`IMGS` 对象中（base64编码）
- **修改方法**：
  1. 将新图片转为 base64（推荐工具：https://www.base64-image.de/）
  2. 替换 `IMGS.lovememo` / `IMGS.personal` / `IMGS.vybcall` 的值
  
#### 项目文本内容
- **存储位置**：`index.html` 内部，`DATA` 对象中
- **修改方法**：直接编辑对应项目的 JSON 配置

#### 项目资源链接（Demo/GitHub/Figma/PRD等）
- **存储位置**：`index.html` 内部，`RESOURCES` 对象中
- **修改方法**：
  1. 找到对应项目（如 `RESOURCES.lovememo`）
  2. 更新对应资源的 `url` 字段
  3. 如果 `url: null` → 显示"Coming soon"占位
  4. 如果 `url: "https://..."` → 显示可点击链接

#### 简历PDF
- **存储位置**：需放在项目目录或 `assets/` 文件夹
- **配置位置**：`SITE.resumePdf`
- **当前状态**：`null`（显示占位按钮）
- **启用方法**：
  1. 将简历 PDF 放到项目目录（如 `Carla_Resume.pdf`）
  2. 修改 `SITE.resumePdf = "Carla_Resume.pdf"`
  3. 刷新页面，Resume 区会显示预览/下载按钮

#### Research / AI Lab / Thinking 文章
- **存储位置**：`index.html` 内部，`ARTICLES` 对象中
- **当前状态**：标题和描述已配置，正文显示"Documentation in progress"
- **修改方法**：编辑 `ARTICLES.research.items.xxx` 条目

---

## E. 后续新增项目的操作方法

### 步骤一：准备项目资料

1. **项目封面图** — 转为 base64 编码
2. **项目文本** — 准备好标题、描述、工作范围、档案章节列表
3. **资源链接** — 收集 Demo、GitHub、Figma、PRD 等链接

### 步骤二：编辑 index.html

打开 `index.html`，定位到 **JavaScript 数据配置区**（约第379行），按以下顺序修改：

#### 1. 添加项目数据（DATA 对象）

```javascript
const DATA = {
  lovememo: {...},
  personal: {...},
  vybcall: {...},
  // 👇 新增项目
  newproject: {
    title: "新项目名称",
    kicker: "分类 · 标签",
    tagline: "一句话介绍项目",
    problem: "项目背景和要解决的问题",
    work: [
      "你的工作内容1",
      "你的工作内容2",
      "你的工作内容3"
    ],
    sections: [
      "项目背景",
      "用户分析",
      "信息架构",
      "原型设计",
      "开发实现",
      "上线成果",
      "产品复盘"
    ]
  }
};
```

#### 2. 添加项目封面图（IMGS 对象）

```javascript
const IMGS = {
  lovememo: "data:image/jpeg;base64,...",
  personal: "data:image/jpeg;base64,...",
  vybcall: "data:image/jpeg;base64,...",
  // 👇 新增项目封面
  newproject: "data:image/jpeg;base64,YOUR_BASE64_HERE"
};
```

#### 3. 添加项目资源（RESOURCES 对象）

```javascript
const RESOURCES = {
  lovememo: [...],
  personal: [...],
  vybcall: [...],
  // 👇 新增项目资源
  newproject: [
    {type:'demo',     label:'Live Demo ↗',    sub:'OPEN LIVE PROJECT', url:'https://your-demo-url.com'},
    {type:'github',   label:'GitHub ↗',       sub:'SOURCE / REPO',     url:'https://github.com/yourname/repo'},
    {type:'figma',    label:'Figma 原型',      sub:'DESIGN FILE',       url:'https://figma.com/file/...'},
    {type:'prd',      label:'PRD / 产品文档',  sub:'COMING SOON',       url:null},  // url:null = 占位
    {type:'prototype',label:'交互原型',        sub:'COMING SOON',       url:null}
  ]
};
```

#### 4. 添加旧版链接（LINKS 对象，保持兼容）

```javascript
const LINKS = {
  lovememo: {...},
  personal: {...},
  vybcall: {...},
  // 👇 新增项目（简化版）
  newproject: {
    demo: "https://your-demo-url.com",
    github: "https://github.com/yourname/repo",
    figma: null
  }
};
```

### 步骤三：添加项目卡片到 Home 页面

定位到 Projects 目录区（约第245-249行），添加新的 `<article>` 卡片：

```html
<div class="project-directory">
  <!-- 现有项目卡片 -->
  <article class="project-entry" onclick="openProject('lovememo')">...</article>
  <article class="project-entry" onclick="openProject('personal')">...</article>
  <article class="project-entry" onclick="openProject('vybcall')">...</article>
  
  <!-- 👇 新增项目卡片 -->
  <article class="project-entry" onclick="openProject('newproject')">
    <div class="project-thumb">
      <img src="data:image/jpeg;base64,YOUR_BASE64_HERE" alt="新项目名称">
    </div>
    <div class="project-entry-body">
      <div class="num">04 / 分类</div>
      <h3>新项目名称</h3>
      <p>项目简短描述（一两句话）</p>
      <span class="entry-link">OPEN CASE STUDY ↗</span>
    </div>
  </article>
</div>
```

### 步骤四：保存并测试

1. 保存 `index.html`
2. 在浏览器中打开（或刷新）
3. 点击新项目卡片，检查详情页是否正常显示
4. 检查资源链接是否正确（有 url 的可点击，null 的显示"Coming soon"）

---

## F. 后续新增文章/实验的操作方法

### Research 文章

编辑 `ARTICLES.research.items`：

```javascript
const ARTICLES = {
  research: {
    kicker: '03 / RESEARCH',
    back: '#research',
    items: {
      xhs: {...},
      moments: {...},
      doubao: {...},
      // 👇 新增研究文章
      newtopic: {
        no: '04',
        title: '新研究文章标题',
        desc: '简短描述这篇研究的内容和结论'
      }
    }
  },
  ...
};
```

然后在 Home 页面的 Research 区域添加对应的行：

```html
<article class="directory-row" role="link" tabindex="0" onclick="openArticle('research','newtopic')">
  <div class="num">04</div>
  <div>
    <h3>新研究文章标题</h3>
    <p>简短描述这篇研究的内容和结论</p>
  </div>
  <div class="go">READ ↗</div>
</article>
```

### AI Lab 实验

编辑 `ARTICLES.lab.items` 和对应的 lab-card：

```javascript
const ARTICLES = {
  lab: {
    kicker: '04 / AI LAB',
    back: '#lab',
    items: {
      meeting: {...},
      travel: {...},
      more: {...},
      // 👇 新增实验
      newexp: {
        no: '04',
        title: '新实验标题',
        desc: '实验的目标和探索方向'
      }
    }
  },
  ...
};
```

```html
<article class="lab-card" role="link" tabindex="0" onclick="openArticle('lab','newexp')">
  <div>
    <div class="lab-no">04 / BUILD</div>
    <h3>新实验标题</h3>
    <p>实验的目标和探索方向</p>
  </div>
  <span>TAG · TAG · TAG</span>
</article>
```

### Thinking 文章

编辑 `ARTICLES.thinking.items` 和对应的 directory-row：

```javascript
const ARTICLES = {
  thinking: {
    kicker: '05 / THINKING',
    back: '#thinking',
    items: {
      memory: {...},
      tech: {...},
      mvp: {...},
      // 👇 新增思考文章
      newthink: {
        no: '04',
        title: '新思考文章标题',
        desc: '这篇文章的核心观点或问题'
      }
    }
  },
  ...
};
```

```html
<article class="directory-row" role="link" tabindex="0" onclick="openArticle('thinking','newthink')">
  <div class="num">04</div>
  <div>
    <h3>新思考文章标题</h3>
    <p>这篇文章的核心观点或问题</p>
  </div>
  <div class="go">ARTICLE ↗</div>
</article>
```

---

## G. 核心文件 — 不要随便修改

### ⚠️ 绝对不要修改

- **`Carla_Portfolio_V3.3.html`** — V3.3 视觉基准备份

### ⚠️ 谨慎修改（仅限数据配置区）

- **`index.html` 第7-196行** — V3.3 原始样式（除非发现 bug，否则不动）
- **`index.html` 第220-367行** — Home 页面 HTML 结构（除非新增项目/文章卡片）
- **`index.html` 第465-523行** — JavaScript 引擎（除非要改功能逻辑）

### ✅ 可以自由修改

- **`index.html` 第379-463行** — 数据配置区（DATA/IMGS/RESOURCES/ARTICLES/SITE）
- 这是内容的唯一来源，修改这里即可更新作品集

---

## H. 常见问题

### Q1: 为什么不迁移到 React/Vite 多文件结构？

**A:** 按照你的指令 十三.4-6：
- V3.3 是视觉基准，迁移会破坏现有设计
- 单文件架构已经足够满足功能需求
- 避免过度工程化（"不要为了看起来专业而重构"）
- 保持"双击即可打开"的简单性

### Q2: 如何替换项目封面图？

**A:** 
1. 使用在线工具将图片转为 base64（推荐：https://www.base64-image.de/）
2. 复制生成的 `data:image/...` 字符串
3. 替换 `IMGS` 对象中对应项目的值
4. 保存并刷新浏览器

### Q3: 如何添加真实的 PRD 文档链接？

**A:**
1. 将 PRD PDF 上传到 GitHub / Google Drive / 其他可访问的位置
2. 获取公开链接
3. 在 `RESOURCES.项目名` 中找到 `type:'prd'` 的条目
4. 将 `url: null` 改为 `url: "你的链接"`
5. 保存并刷新

### Q4: 如何启用 Resume PDF 预览/下载？

**A:**
1. 将简历 PDF 文件放到项目根目录（如 `Carla_Resume.pdf`）
2. 编辑 `SITE.resumePdf = "Carla_Resume.pdf"`
3. 保存并刷新，Resume 区会显示预览和下载按钮

### Q5: 移动端导航菜单有些项目点不到？

**A:** 已修复！V3.4 的移动端样式让导航横向滚动，所有菜单项都可以访问了。

### Q6: 如何添加新的资源类型（如"用户测试报告"）？

**A:** 在 `RESOURCES.项目名` 数组中添加新条目：

```javascript
{
  type: 'testing',
  label: '用户测试报告',
  sub: 'USER TESTING',
  url: 'https://your-report-link.com'  // 或 null
}
```

### Q7: 文章详情页目前都是占位，如何添加真实内容？

**A:** 当前版本的文章详情页显示"Documentation in progress"。要添加真实内容，需要：
1. 修改 `renderArticle()` 函数（第477-487行）
2. 为每篇文章添加 `content` 字段到 `ARTICLES` 配置
3. 或者连接到外部 Markdown/HTML 文件

（建议：等真正写完文章再做，现在保持诚实的占位状态）

---

## I. 开发环境

### 如何运行项目

**方法一：直接双击**
- 双击 `index.html` 即可在浏览器中打开
- 适用于大部分现代浏览器

**方法二：本地服务器（推荐）**
```bash
# Python 3
python -m http.server 8000

# Node.js (需要先安装 http-server)
npx http-server -p 8000
```
然后访问 `http://localhost:8000`

### 浏览器兼容性

- ✅ Chrome / Edge（推荐）
- ✅ Firefox
- ✅ Safari
- ⚠️ IE11 及以下不支持

### 响应式设计

- ✅ Desktop (>800px)
- ✅ Mobile (<800px)
- 已针对移动端优化：导航菜单横向滚动，详情页单列布局

---

## J. 版本历史

### V3.4（2026-09-08）
- ✅ 基于 V3.3 增量开发（保持视觉设计不变）
- ✅ 修复 Research / AI Lab / Thinking 死链接
- ✅ 添加项目详情页（全页视图，13档案结构）
- ✅ 添加文章详情页（诚实占位）
- ✅ 数据驱动架构（RESOURCES / ARTICLES / SITE 配置）
- ✅ 真实资源链接（Demo / GitHub）+ 诚实占位（Figma / PRD）
- ✅ Resume 区动态配置（PDF 路径）
- ✅ 移动端导航修复（横向滚动）
- ✅ Back-to-top 按钮
- ✅ Hash 路由（`#/p/lovememo` / `#/a/research/xhs`）

### V3.3（基准版本）
- ✅ Home 长滚动页面（Hero / Projects / Research / Lab / Thinking / About）
- ✅ 项目模态弹窗（已在 V3.4 改为全页详情）
- ✅ Editorial 风格设计（黑白主视觉、装饰元素、字体体系）

---

## K. 联系与支持

如有问题，可以：
1. 检查浏览器控制台是否有 JavaScript 错误
2. 确认修改的 JSON 配置语法正确（逗号、引号、括号）
3. 对比 V3.3 备份文件，确认没有误删关键内容
4. 参考本文档的"后续操作方法"部分

---

**文档版本：** V3.4  
**最后更新：** 2026-09-08  
**作者：** Carla / 咕咕 + Claude Code
